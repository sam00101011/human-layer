{
  "name": "@human-layer/db",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "corepack pnpm exec tsc -p tsconfig.json --noEmit",
    "migrate": "corepack pnpm exec tsx src/migrate.ts",
    "seed:foundation": "corepack pnpm exec tsx src/seed-foundation.ts",
    "test": "corepack pnpm exec vitest run"
  },
  "dependencies": {
    "@human-layer/core": "workspace:*",
    "drizzle-orm": "^0.43.1",
    "pg": "^8.14.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.1"
  }
}
